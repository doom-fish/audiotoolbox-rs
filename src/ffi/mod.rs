/// Bindings for the `AUAudioUnit` portion of AudioToolbox.framework.
pub mod au_audio_unit;
/// Bindings for the `AUGraph` portion of AudioToolbox.framework.
pub mod au_graph;
/// Bindings for the `AudioComponent` portion of AudioToolbox.framework.
pub mod audio_component;
/// Bindings for the `AudioConverter` portion of AudioToolbox.framework.
pub mod audio_converter;
/// Bindings for the `AudioFile` portion of AudioToolbox.framework.
pub mod audio_file;
/// Bindings for the `AudioFileComponent` portion of AudioToolbox.framework.
pub mod audio_file_component;
/// Bindings for the `AudioFileStream` portion of AudioToolbox.framework.
pub mod audio_file_stream;
/// Bindings for the `AudioFormat` portion of AudioToolbox.framework.
pub mod audio_format;
/// Bindings for the `AudioQueue` portion of AudioToolbox.framework.
pub mod audio_queue;
/// Bindings for the `AudioServices` portion of AudioToolbox.framework.
pub mod audio_services;
/// Bindings for the `AudioUnit` portion of AudioToolbox.framework.
pub mod audio_unit;
/// Bindings for the `AVFAudio` portion of AudioToolbox.framework.
pub mod avfaudio;
/// Bindings for the `CAFFile` portion of AudioToolbox.framework.
pub mod caf_file;
/// Bindings for the `CoreAudio` portion of AudioToolbox.framework.
pub mod core;
/// Bindings for the `ExtAudioFile` portion of AudioToolbox.framework.
pub mod ext_audio_file;
/// Bindings for the `Music` portion of AudioToolbox.framework.
pub mod music;
